project('mcanim_vk', 'cpp')

fmt_dep = dependency('fmt')
glfw_dep = dependency('glfw3')
vulkan_dep = dependency('vulkan')

executable('mcanim', 'src/main.cpp',
           dependencies: [fmt_dep, glfw_dep, vulkan_dep])
